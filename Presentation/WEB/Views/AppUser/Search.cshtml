@model List<AppUser>
<div id="content-page" class="content-page">
    <div class="container">
        <div class="row">
            @if (Model.Any())
            {
                @foreach (var user in Model)
                {
                    <div class="col-lg-6">
                        <a asp-controller="Profile" asp-action="User" asp-route-username="@user.UserName">
                            <div class="card iq-mb-3">
                                <div class="row no-gutters">
                                    <div class="col-md-4">
                                        <img src="~/assets/images/@user.MainImage" class="card-img" alt="#">
                                    </div>
                                    <div class="col-md-8">
                                        <div class="card-body">
                                            <h4 class="card-title">@user.Name @user.Surname</h4>
                                            <p class="card-text"><small class="text-muted">@user.UserName</small></p>
                                            <p class="card-text">@user.SelfInformation</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </a>
                    </div>
                }
            }
            else
            {
                <div class="iq-header-title">
                    <h4 class="card-title">User not found.</h4>
                </div>
            }
            
        </div>
    </div>
</div>